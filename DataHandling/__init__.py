"""
Top-level package for the CreditLoan backend.

This file makes the `DataHandling` directory importable as a Python package,
which is required for module paths like `DataHandling.wsgi` when running
under production WSGI servers (e.g. Waitress on Railway).
"""


